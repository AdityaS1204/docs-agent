module.exports = {
    // Wrapper for Google Docs API calls if moving logic to backend
    fetchDocument: async (docId) => {
        console.log(`Fetching document ${docId}`);
        return {};
    }
};
